<% @pid = @users.id%>
<%= link_to "<<Go back to Profile", {:action => 'show', :id => @users.id} %>

<div>
  <h2>Edit Your Profile</h2>

  <%= form_for(@users) do |f| %>

    <table>
      <tr>
        <th>Edit your First Name here: &nbsp;</th>
        <td><%= f.text_field(:first_name) %></td>
      </tr>
      <tr>
        <th>Edit you Last Name here: </th>
        <td><%= f.text_field(:last_name) %></td>
      </tr>
      <tr>
        <th>Edit your bio here: </th> 
        <td><%= f.text_area(:bio, :size => "30x10") %></td>
      </tr>
      
    </table>

    <div class="form buttons">
      <%= submit_tag("Update profile", :class => "btn btn-primary) %>
    </div>

  <% end %>
</div>




